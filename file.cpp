#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef pair<int, int> ii;
typedef vector<int> vi;
typedef vector<ii> vii;

int tempInt;
long long tempLong;
string tempString;
char tempChar;
int maxi = INT_MIN;
int mini = INT_MAX;
int caseNum = 1;

// DP
//vector<vector<int>> dp2d(row_count, vector<int>(column_count, initial_value));
//vector<vector<int>> dp2d(100, vector<int>(100, -1));
//vector<int> dp1d(col_count, initial_value);
//vector<int> dp1d(100,-1);

bool isOdd(int number)
{
	return number % 2 != 0;
}

// False implies a swap, True implies correct
bool compare(int lhs, int rhs)
{
}

int main()
{
	
}
